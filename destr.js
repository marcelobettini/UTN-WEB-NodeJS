const horse = {
  nome: "A horse with no name",
  age: 6,
  size: "Large",
  skills: ["racing", "jousting"],
};

console.log(horse);
const { nome, age, size, skills } = horse;
console.log(nome);
console.log(horse.nome);